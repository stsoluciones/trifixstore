import RegisterForm from '@/features/auth/components/RegisterForm'

export const metadata = { title: 'Crear cuenta — Trifixstore' }

export default function RegisterPage() {
  return <RegisterForm />
}
