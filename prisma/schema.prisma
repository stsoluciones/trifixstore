// ⚠️  ESTE ARCHIVO ES SOLO UNA REFERENCIA DE ESQUEMA — NO SE USA EN PRODUCCIÓN
//
// El proyecto usa el driver nativo de MongoDB (paquete `mongodb`) sin Prisma ORM.
// La capa de acceso a datos está en:
//   src/shared/lib/mongodb.ts        → singleton de conexión
//   src/shared/lib/db/types.ts       → interfaces TypeScript de documentos
//   src/shared/lib/db/collections.ts → getters tipados + crearIndices()
//   src/features/*/services/         → lógica CRUD de cada feature
//
// Colecciones MongoDB:
//   usuarios   → UsuarioDoc
//   categorias → CategoriaDoc
//   productos  → ProductoDoc
//   ordenes    → OrdenDoc
